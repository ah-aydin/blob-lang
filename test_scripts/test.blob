func main(): i32 {
  var a: i32 = 1; # 0
  var b: i32 = 2; # 4
  var c: i32 = 3; # 8
  var d: i32 = 4; # 12
  var e: i32 = 5; # 16
  var f: i32 = 6; # 20
  var i: i32 = 0;
  var sum: i32 = 0;
  while (i < 4) {
    var i_power: i32 = i * i;
    var new_sum: i32 = sum + i_power;
    sum = new_sum;
    i = i + 1;
  }
  return divide(3, 20);
  return a + b + c + d + e + f;
}
